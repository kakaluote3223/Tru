<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.computer.hdu.truckrental">

    <!-- To auto-complete the email text field in the login form with the btn_home_user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.Base">
        <activity
            android:name=".UserLoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                 <action android:name="android.intent.action.MAIN" />

                 <category android:name="android.intent.category.LAUNCHER" />
                <!--<category android:name="android.intent.category.DEFAULT" />-->
            </intent-filter>
        </activity>
        <activity
            android:name=".DriverLoginActivity"
            android:label="@string/label_driver_login">
            <intent-filter>

                <!-- <action android:name=""/> -->
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".UserRegisterActivity"
            android:label="@string/label_register_user">
            <intent-filter>

                <!-- <action android:name=""/> -->
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DriverRegisterActivity"
            android:label="@string/label_register_driver">
            <intent-filter>

                <!-- <action android:name=""/> -->
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DriverRuleShowActivity"
            android:label="@string/label_driver_rule">
            <intent-filter>

                <!-- <action android:name=""/> -->
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".OrderCreateActivity"
            android:label="@string/label_order_create">
            <intent-filter>

                <!-- <action android:name=""/> -->
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity android:name=".LocationSelectActivity" />
        <activity
            android:name=".DriverAllOrdersShowActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/driver_all_orders">
            <intent-filter>

                <!-- <action android:name=""/> -->
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DriverAvailableOrderShowActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/label_main_driver">
            <intent-filter>

                <!-- <action android:name=""/> -->
                <!--<action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />-->
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".DriverOrderDetailsShowActivity"
            android:label="@string/order_details">
            <intent-filter>

                <!-- <action android:name=""/> -->
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DriverPriceDetailsShowActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/price_details">
            <intent-filter>

                <!-- <action android:name=""/> -->
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity android:name=".DriverRunningOrdersDetailsShowActivity" />
        <activity android:name=".DriverAllOrdersDetailsShowActivity" />
        <activity
            android:name=".DriverRunningOrdersShowActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/driver_completed_orders">
            <intent-filter>

                <!-- <action android:name=""/> -->
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity android:name=".DriverInfoShowActivity" />
        <activity android:name=".DriverChangePwdActivity"></activity>
    </application>

</manifest>